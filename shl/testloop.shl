// test sub calls

// declare some
var a <- 42; 
var b <- 69; 
var c <- 96;
var i;

main {
  i <- 69;
  if(i > 42) {
    i <- 2*a+b*c-3;
  }
  
  i <- 0;
  loop {
    i <- i + 1;
    stop on(i = 3);
  }
}

// EOF

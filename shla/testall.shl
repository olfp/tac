// test shla

var i <- 0; // foo
var n;
var j, l;
var k <- 42, m <- 7;

sub odd {
  i <- 1;
}

sub even {
  i <- 0;
}

main {
  loop {
    if(i = 0) {
      call odd;
    } else {
      call even;
    }
    j <- j + 1;
    stop on(j > 9);
  }
}

// EOF

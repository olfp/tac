// select odd number

var n, res[10], i;

data 1,2,3,4,5,6,7,-1;

main {
     i <- 0;
     loop {
     	  read n;
	  stop on(n < 0);
	  if(n <> (n / 2) * 2) {
	       res[i] <- n;
	       i <- i + 1;
	  }
     }
}

// end
